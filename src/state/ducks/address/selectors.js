export const isGetAddress = (state) => {
  return state.address.getAddress;
};

export const isAddAddress = (state) => {
  return state.address.addAddress;
};

export const isDeleteAddress = (state) => {
  return state.address.deleteAddress;
};
